# Copy this file to .env and fill in your local values.

# Postgres connection string. Leave as-is if your local Postgres accepts
# connections to this database without a username/password.
DATABASE_URL=postgresql://localhost:5432/property_ai_poc

# Required only for the LLM retrieval planner. If this is blank or missing,
# the app uses the deterministic fallback planner.
OPENAI_API_KEY=

# Optional. Change this if you want to test another OpenAI model.
OPENAI_MODEL=gpt-5.5
